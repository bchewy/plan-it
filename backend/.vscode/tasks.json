{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    // press run & debug , then press play button to run the webapp backend
    "version": "2.0.0",
    "tasks": [
        {
            "label": "install",
            "type": "shell",
            "command": "pip install -r requirements.txt",
            "group": {
				"kind": "build"
			}
        }
    ]
}